# Salesforce credentials used by the login task (never commit real values)
SF_USERNAME=your.user@example.com
SF_PASSWORD=your-password

# Optional: run Chrome headless (default: false)
HEADLESS=false
